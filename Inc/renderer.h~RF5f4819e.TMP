#ifndef __RENDERER_H
#define __RENDERER_H

#include <vector>

#include "layer.h"

extern "C" {
	#include "display.h"
}

#define MAX_LAYERS 16

class Renderer {	
	public:
		Renderer();
		
		void flush(void);
		void clr_layers(void);
		Renderer& operator<<(Layer &layer);
	
	private:
		uint8_t m_layer_ptr;
		Bitmap m_buffer;
};

#endif
